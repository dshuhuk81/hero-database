---
export const prerender = true;
import Base from "../layouts/Base.astro";
import allHeroes from "../data/all_heroes_db.json";
import heroRatings from "../data/ratings/hero-ratings.json";
import { heroes as heroPages } from "../data/heroes/withRatings";

type Status = "released" | "coming-soon" | "far-future";

interface ScheduleEntry {
  id: string;
  displayName?: string;
  status: Status;
  order: number;
}

const schedule: ScheduleEntry[] = [
  { id: "hladgunnr",     status: "released",    order: 1 },
  { id: "nezha",         status: "released",    order: 2 },
  { id: "tefnut",        status: "released",    order: 3 },
  { id: "heracles",      status: "released",    order: 4 },
  { id: "nuba",          status: "released",    order: 5 },
  { id: "mengpo",        status: "released",    order: 6 },
  { id: "skadi",         status: "released",    order: 7 },
  { id: "cronus",        status: "released",    order: 8 },
  { id: "wenshen",       status: "released",    order: 9 },
  { id: "meret",         status: "released",    order: 10 },
  { id: "hephaestus",    status: "released", order: 11 },
  { id: "serket",        status: "released", order: 12 },
  { id: "hera",          status: "released", order: 13 },
  { id: "eris",          status: "released", order: 14 },
  { id: "nut",          status: "released", order: 15 },
  { id: "jiutianxuannv",       status: "coming-soon", order: 16 },
  { id: "nephtys",          status: "coming-soon", order: 17 },
  { id: "eden",      status: "coming-soon", order: 18 },
  { id: "hades",      status: "far-future", order: 19 },
  { id: "audhumla",         status: "far-future", order: 20 },
  { id: "hebo",         status: "far-future", order: 21 },
  { id: "heket",         status: "far-future", order: 22 },
  { id: "zaojun",         status: "far-future", order: 23 },
];

const heroMap = new Map(Object.values(allHeroes as Record<string, any>).map((h: any) => [h.id, h]));
const heroPageIds = new Set((heroPages as any[]).map((h) => h.id));

const R2_BASE = "https://pub-a33abfbc3135413881a1d8eb86543559.r2.dev";
const HERO_PLACEHOLDER = `${R2_BASE}/heroes/placeholder.png`;
const HERO_IMAGE_VERSION = "2026-06-08-portrait";
const SPOTLIGHT_IMAGE_SIZES = "(max-width: 400px) calc(100vw - 40px), (max-width: 600px) calc((100vw - 56px) / 2), 220px";

function heroImg(id: string): string {
  return `${R2_BASE}/heroes/${encodeURIComponent(id)}.webp`;
}

function heroCardImg(id: string, width = 360): string {
  return `${R2_BASE}/heroes/cards/${encodeURIComponent(id)}-${width}.webp?v=${HERO_IMAGE_VERSION}`;
}

function heroCardSrcset(id: string): string {
  const encodedId = encodeURIComponent(id);
  return [240, 360, 480]
    .map((width) => `${R2_BASE}/heroes/cards/${encodedId}-${width}.webp?v=${HERO_IMAGE_VERSION} ${width}w`)
    .join(", ");
}

function heroThumbImg(id: string): string {
  return `${R2_BASE}/heroes/thumbs/${encodeURIComponent(id)}-96.webp?v=${HERO_IMAGE_VERSION}`;
}

const iconUrl = (type: string, value: string) =>
  value ? `${R2_BASE}/icons/${type}/${value.toLowerCase()}.webp` : null;

function resolveEntry(entry: ScheduleEntry) {
  const h = heroMap.get(entry.id) || {};
  const r = (heroRatings as Record<string, any>)?.[entry.id] ?? {};
  const faction = h.faction || "";
  const heroClass = h.class || "";
  const role = h.role || "";
  return {
    id: entry.id,
    displayName: entry.displayName || h.name || entry.id,
    faction,
    heroClass,
    role,
    classIconUrl: heroClass ? iconUrl("classes", heroClass) : null,
    factionIconUrl: faction ? iconUrl("factions", faction) : null,
    roleIconUrl: role ? iconUrl("roles", role) : null,
    rarity: h.rarity || "Legendary",
    overallRating: r.overall || "?",
    pvpRating: r.pvp || "?",
    pveRating: r.pve || "?",
    status: entry.status,
    order: entry.order,
    hasDetailPage: heroPageIds.has(entry.id),
  };
}

const released = schedule.filter(e => e.status === "released").map(resolveEntry);
const comingSoon = schedule.filter(e => e.status === "coming-soon").map(resolveEntry);
const farFuture = schedule.filter(e => e.status === "far-future").map(resolveEntry);

---

<Base
  title="Summon Calendar - Motto Immortal"
  description="Track upcoming hero releases for Motto Immortal. See what heroes are coming soon based on extracted game data."
>
  <main class="page-wrap">

    <header class="page-header">
      <div class="header-deco"></div>
      <div class="header-content">
        <div class="header-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
            <line x1="16" y1="2" x2="16" y2="6"/>
            <line x1="8" y1="2" x2="8" y2="6"/>
            <line x1="3" y1="10" x2="21" y2="10"/>
            <circle cx="8" cy="16" r="1.5" fill="currentColor"/>
            <circle cx="12" cy="16" r="1.5" fill="currentColor"/>
            <circle cx="16" cy="16" r="1.5" fill="currentColor"/>
          </svg>
        </div>
        <div>
          <h1 class="page-title">Summon Calendar</h1>
          <p class="page-subtitle">Possible upcoming heroe1s.</p>
        </div>
      </div>
      <div class="disclaimer">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M12 8h.01"/><path d="M11 12h1v4h1"/>
        </svg>
        Order could be inaccurate and is based on data mined from CN servers. Take it with a grain of salt!
      </div>
    </header>

    <!-- COMING SOON SPOTLIGHT -->
    <section class="section">
      <h2 class="section-title">
        <span class="section-dot dot-soon"></span>
        Coming Next
      </h2>
      <div class="spotlight-grid">
        {comingSoon.map((hero, i) => (
          <a href={`/heroes/${hero.id}`} class="spotlight-card">
            <div class="spotlight-order">{hero.order}</div>
            <div class="spotlight-img-wrap">
              <img
                class="spotlight-img"
                src={heroCardImg(hero.id)}
                srcset={heroCardSrcset(hero.id)}
                sizes={SPOTLIGHT_IMAGE_SIZES}
                alt={hero.displayName}
                width="360"
                height="360"
                decoding="async"
                fetchpriority={i === 0 ? "high" : "low"}
                onerror={`this.onerror=()=>{this.onerror=null;this.src='${HERO_PLACEHOLDER}';this.parentElement.classList.add('img-missing')};this.srcset='';this.src='${heroImg(hero.id)}'`}
              />
              <div class="spotlight-img-fade"></div>
            </div>
            <div class="spotlight-body">
              <div class="hero-meta-row">
                {hero.classIconUrl ? <img src={hero.classIconUrl} alt={hero.heroClass} class="meta-icon" title={hero.heroClass} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.heroClass}>?</span>}
                {hero.factionIconUrl ? <img src={hero.factionIconUrl} alt={hero.faction} class="meta-icon" title={hero.faction} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.faction}>?</span>}
                {hero.roleIconUrl ? <img src={hero.roleIconUrl} alt={hero.role} class="meta-icon" title={hero.role} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.role}>?</span>}
              </div>
              <h3 class="spotlight-name">{hero.displayName}</h3>
              <p class="hero-role">{hero.role}</p>
            </div>

          </a>
        ))}
      </div>
    </section>

    <!-- FAR FUTURE --><!-- 
    {farFuture.length > 0 && (
      <section class="section">
        <h2 class="section-title">
          <span class="section-dot dot-future"></span>
          Far Future
        </h2>
        <div class="spotlight-grid">
          {farFuture.map((hero) => (
            <a href={`/heroes/${hero.id}`} class="spotlight-card future-card">
              <div class="spotlight-order">{hero.order}</div>
              <div class="spotlight-img-wrap">
                <div
                  class="spotlight-img"
                  style={`background-image: url('${heroImg(hero.id)}');`}
                ></div>
                <div class="spotlight-img-fade"></div>
              </div>
              <div class="spotlight-body">
                <div class="hero-meta-row">
                  <img src={iconUrl("classes", hero.heroClass)} alt={hero.heroClass} class="meta-icon" title={hero.heroClass} />
                  <img src={iconUrl("factions", hero.faction)} alt={hero.faction} class="meta-icon" title={hero.faction} />
                  <img src={iconUrl("roles", hero.role)} alt={hero.role} class="meta-icon" title={hero.role} />
                </div>
                <h3 class="spotlight-name">{hero.displayName}</h3>
                <p class="hero-role">{hero.role}</p>
                </div>
              <div class="spotlight-next-badge future-badge">FAR FUTURE</div>
            </a>
          ))}
        </div>
      </section>
    )}-->

    <!-- FULL TIMELINE -->
    <section class="section">
      <h2 class="section-title">
        <span class="section-dot dot-all"></span>
        Full Release Timeline
      </h2>
      <div class="timeline">
        <!-- released group -->
        <div class="timeline-group-label">
          <span class="tgl-dot released-dot"></span>
          Already Released
        </div>
        {released.map((hero) => (
          <a href={`/heroes/${hero.id}`} class="timeline-row released-row">
            <div class="tl-order">{hero.order}</div>
            <img
              class="tl-portrait"
              src={heroThumbImg(hero.id)}
              alt={hero.displayName}
              width="44"
              height="44"
              loading="lazy"
              decoding="async"
              onerror={`this.onerror=()=>{this.style.display='none';this.insertAdjacentHTML('afterend','<div class=\\'tl-portrait tl-portrait-missing\\'>?</div>')};this.src='${heroImg(hero.id)}'`}
            />
            <div class="tl-info">
              <span class="tl-name">{hero.displayName}</span>
              <span class="tl-sub">
                {hero.classIconUrl ? <img src={hero.classIconUrl} alt={hero.heroClass} class="tl-icon" title={hero.heroClass} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.heroClass}>?</span>}
                {hero.roleIconUrl ? <img src={hero.roleIconUrl} alt={hero.role} class="tl-icon" title={hero.role} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.role}>?</span>}
              </span>
            </div>
            <div class="tl-faction">
              {hero.factionIconUrl ? <img src={hero.factionIconUrl} alt={hero.faction} class="tl-icon" title={hero.faction} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.faction}>?</span>}
            </div>
            <div class="tl-ratings">
              <span class="tl-pill" data-tier={hero.overallRating}>{hero.overallRating}</span>
            </div>
            <div class="tl-status-badge released-badge">Released</div>
          </a>
        ))}

        <!-- coming soon group -->
        <div class="timeline-group-label">
          <span class="tgl-dot soon-dot"></span>
          Coming Soon
        </div>
	        {comingSoon.map((hero) => (
	          <a href={`/heroes/${hero.id}`} class="timeline-row soon-row">
	            <div class="tl-order">{hero.order}</div>
	            <img
                class="tl-portrait"
                src={heroThumbImg(hero.id)}
                alt={hero.displayName}
                width="44"
                height="44"
                loading="lazy"
                decoding="async"
                onerror={`this.onerror=()=>{this.onerror=null;this.src='${HERO_PLACEHOLDER}'};this.src='${heroImg(hero.id)}'`}
              />
            <div class="tl-info">
              <span class="tl-name">{hero.displayName}</span>
              <span class="tl-sub">
                {hero.classIconUrl ? <img src={hero.classIconUrl} alt={hero.heroClass} class="tl-icon" title={hero.heroClass} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.heroClass}>?</span>}
                {hero.roleIconUrl ? <img src={hero.roleIconUrl} alt={hero.role} class="tl-icon" title={hero.role} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.role}>?</span>}
              </span>
            </div>
            <div class="tl-faction">
              {hero.factionIconUrl ? <img src={hero.factionIconUrl} alt={hero.faction} class="tl-icon" title={hero.faction} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.faction}>?</span>}
            </div>
            <div class="tl-ratings" aria-hidden="true"></div>
            <div class="tl-status-badge soon-badge">Soon</div>
          </a>
        ))}

        <!-- far future group -->
        {farFuture.length > 0 && (
          <>
            <div class="timeline-group-label">
              <span class="tgl-dot future-tgl-dot"></span>
              Far Future
            </div>
            {farFuture.map((hero) => (
              hero.hasDetailPage ? (
                <a href={`/heroes/${hero.id}`} class="timeline-row future-row">
                  <div class="tl-order">{hero.order}</div>
                  <img
                    class="tl-portrait future-portrait"
                    src={heroThumbImg(hero.id)}
                    alt={hero.displayName}
                    width="44"
                    height="44"
                    loading="lazy"
                    decoding="async"
                    onerror={`this.onerror=()=>{this.onerror=null;this.src='${HERO_PLACEHOLDER}'};this.src='${heroImg(hero.id)}'`}
                  />
                  <div class="tl-info">
                    <span class="tl-name">{hero.displayName}</span>
                    <span class="tl-sub">
                      {hero.classIconUrl ? <img src={hero.classIconUrl} alt={hero.heroClass} class="tl-icon" title={hero.heroClass} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.heroClass}>?</span>}
                      {hero.roleIconUrl ? <img src={hero.roleIconUrl} alt={hero.role} class="tl-icon" title={hero.role} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.role}>?</span>}
                    </span>
                  </div>
                  <div class="tl-faction">
                    {hero.factionIconUrl ? <img src={hero.factionIconUrl} alt={hero.faction} class="tl-icon" title={hero.faction} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.faction}>?</span>}
                  </div>
                  <div class="tl-ratings" aria-hidden="true"></div>
                  <div class="tl-status-badge future-badge-tl">Far Future</div>
                </a>
              ) : (
                <div class="timeline-row future-row is-disabled" aria-disabled="true" title="No hero detail page yet">
                  <div class="tl-order">{hero.order}</div>
                  <img
                    class="tl-portrait future-portrait"
                    src={heroThumbImg(hero.id)}
                    alt={hero.displayName}
                    width="44"
                    height="44"
                    loading="lazy"
                    decoding="async"
                    onerror={`this.onerror=()=>{this.onerror=null;this.src='${HERO_PLACEHOLDER}'};this.src='${heroImg(hero.id)}'`}
                  />
                  <div class="tl-info">
                    <span class="tl-name">{hero.displayName}</span>
                    <span class="tl-sub">
                      {hero.classIconUrl ? <img src={hero.classIconUrl} alt={hero.heroClass} class="tl-icon" title={hero.heroClass} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.heroClass}>?</span>}
                      {hero.roleIconUrl ? <img src={hero.roleIconUrl} alt={hero.role} class="tl-icon" title={hero.role} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.role}>?</span>}
                    </span>
                  </div>
                  <div class="tl-faction">
                    {hero.factionIconUrl ? <img src={hero.factionIconUrl} alt={hero.faction} class="tl-icon" title={hero.faction} onerror="this.style.display='none';this.insertAdjacentHTML('afterend','<span class=\'icon-fallback\'>?</span>')" /> : <span class="icon-fallback" title={hero.faction}>?</span>}
                  </div>
                  <div class="tl-ratings" aria-hidden="true"></div>
                  <div class="tl-status-badge future-badge-tl">Far Future</div>
                </div>
              )
            ))}
          </>
        )}
      </div>
    </section>

  </main>
</Base>

<style>
  .page-wrap {
    max-width: 960px;
    margin: 0 auto;
    padding: 32px 20px 80px;
  }

  /* ---- Header ---- */
  .page-header {
    position: relative;
    background: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    border-radius: 16px;
    padding: 28px 28px 20px;
    margin-bottom: 40px;
    overflow: hidden;
  }

  .header-deco {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 80% 0%, rgba(250, 204, 21, 0.07) 0%, transparent 60%),
                radial-gradient(ellipse at 20% 100%, rgba(168, 85, 247, 0.05) 0%, transparent 60%);
    pointer-events: none;
  }

  .header-content {
    display: flex;
    align-items: center;
    gap: 18px;
    position: relative;
  }

  .header-icon {
    width: 52px;
    height: 52px;
    background: rgba(250, 204, 21, 0.1);
    border: 1px solid rgba(250, 204, 21, 0.2);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--accent-gold);
  }

  .header-icon svg {
    width: 26px;
    height: 26px;
  }

  .page-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text-bright);
    margin: 0 0 4px;
    letter-spacing: -0.01em;
  }

  .page-subtitle {
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.4;
  }

  .disclaimer {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-top: 16px;
    padding: 9px 14px;
    background: rgba(250, 204, 21, 0.05);
    border: 1px solid rgba(250, 204, 21, 0.12);
    border-radius: 8px;
    font-size: 0.78rem;
    color: rgba(250, 204, 21, 0.7);
    position: relative;
  }

  /* ---- Section ---- */
  .section {
    margin-bottom: 48px;
  }

  .section-title {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text-bright);
    margin: 0 0 20px;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }

  .section-dot {
    display: inline-block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .dot-soon {
    background: #facc15;
    box-shadow: 0 0 8px rgba(250, 204, 21, 0.6);
    animation: pulse-dot 2s ease-in-out infinite;
  }

  .dot-future { background: #a855f7; }
  .dot-all    { background: var(--text-muted); }

  @keyframes pulse-dot {
    0%, 100% { opacity: 1; box-shadow: 0 0 8px rgba(250, 204, 21, 0.6); }
    50%       { opacity: 0.6; box-shadow: 0 0 4px rgba(250, 204, 21, 0.2); }
  }

  /* ---- Spotlight Grid ---- */
  .spotlight-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 16px;
  }

  .spotlight-card {
    position: relative;
    background: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    border-radius: 14px;
    overflow: hidden;
    text-decoration: none;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
    cursor: pointer;
    border-color: rgba(var(--faction-color-rgb, 250, 204, 21), 0.2);
    box-shadow: 0 0 0 0 transparent;
  }

  .spotlight-card:hover {
    transform: translateY(-4px);
    border-color: var(--faction-color);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4), 0 0 20px rgba(var(--faction-color-rgb, 250, 204, 21), 0.12);
  }

  .future-card {
    opacity: 0.65;
    filter: saturate(0.5);
  }

  .future-card:hover {
    opacity: 0.9;
    filter: saturate(1);
  }

  .spotlight-order {
    position: absolute;
    top: 10px;
    left: 10px;
    z-index: 3;
    width: 26px;
    height: 26px;
    background: rgba(7, 6, 12, 0.75);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--text-secondary);
    backdrop-filter: blur(4px);
  }

  .spotlight-img-wrap {
    position: relative;
    width: 100%;
    padding-top: 100%;
    overflow: hidden;
    background: var(--bg-raised);
  }

  .spotlight-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    transition: transform 0.35s ease;
  }

  .spotlight-img-wrap.img-missing::after {
    content: "?";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--text-muted);
  }

  .spotlight-card:hover .spotlight-img {
    transform: scale(1.06);
  }

  .spotlight-img-fade {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(to top, var(--bg-surface), transparent);
    pointer-events: none;
  }

  .spotlight-body {
    padding: 12px 14px 14px;
    display: flex;
    flex-direction: column;
    gap: 6px;
    flex: 1;
  }

  .hero-meta-row {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
  }

  .meta-icon {
    width: 22px;
    height: 22px;
    object-fit: contain;
    flex-shrink: 0;
  }

  .icon-fallback {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 20px;
    height: 20px;
    padding: 0 5px;
    border-radius: 4px;
    background: linear-gradient(135deg, #6b7280, #374151);
    font-size: 0.65rem;
    font-weight: 800;
    color: #f3f4f6;
    flex-shrink: 0;
    letter-spacing: 0.03em;
    line-height: 1;
  }

  .spotlight-name {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text-bright);
    margin: 0;
    line-height: 1.2;
  }

  .hero-role {
    font-size: 0.75rem;
    color: var(--text-muted);
    margin: 0;
  }

  .hero-note {
    font-size: 0.7rem;
    color: rgba(168, 85, 247, 0.8);
    margin: 0;
    font-style: italic;
    background: rgba(168, 85, 247, 0.06);
    border-radius: 4px;
    padding: 3px 6px;
  }

  .rating-row {
    display: flex;
    align-items: center;
    gap: 5px;
    margin-top: 4px;
    flex-wrap: wrap;
  }

  .rating-label {
    font-size: 0.65rem;
    color: var(--text-muted);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .rating-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 32px;
    height: 22px;
    padding: 0 7px;
    border-radius: 5px;
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.03em;
    line-height: 1;
  }

  .rating-pill.small {
    min-width: 26px;
    height: 18px;
    font-size: 0.68rem;
  }

  .spotlight-next-badge {
    position: absolute;
    top: 10px;
    right: 10px;
    z-index: 3;
    font-size: 0.6rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    padding: 3px 7px;
    border-radius: 4px;
    background: var(--accent-gold);
    color: #1a1100;
    text-transform: uppercase;
  }

  .future-badge {
    background: rgba(168, 85, 247, 0.85);
    color: #fff;
  }

  /* ---- Timeline ---- */
  .timeline {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .timeline-group-label {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    padding: 16px 0 6px;
    border-bottom: 1px solid var(--border-faint);
    margin-bottom: 2px;
  }

  .tgl-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .released-dot   { background: #6b7280; }
  .soon-dot       { background: #facc15; box-shadow: 0 0 6px rgba(250, 204, 21, 0.5); }
  .future-tgl-dot { background: #a855f7; }

  .timeline-row {
    display: grid;
    grid-template-columns: 32px 48px 1fr auto auto auto;
    align-items: center;
    gap: 12px;
    padding: 10px 14px;
    border-radius: 10px;
    text-decoration: none;
    transition: background 0.15s ease;
    border: 1px solid transparent;
  }

  .timeline-row:hover {
    background: var(--bg-raised);
    border-color: var(--border-subtle);
  }

  .released-row { opacity: 0.6; }
  .released-row:hover { opacity: 1; }

  .future-row { opacity: 0.45; filter: saturate(0.4); }
  .future-row:hover { opacity: 0.85; filter: saturate(1); }
  .timeline-row.is-disabled { cursor: default; }
  .timeline-row.is-disabled:hover { background: transparent; border-color: transparent; opacity: 0.45; filter: saturate(0.4); }

  .tl-order {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--text-muted);
    text-align: center;
  }

  .tl-portrait {
    width: 44px;
    height: 44px;
    border-radius: 8px;
    object-fit: cover;
    object-position: center top;
    border: 1px solid var(--border-subtle);
    flex-shrink: 0;
  }

  .tl-portrait-missing {
    display: flex !important;
    align-items: center;
    justify-content: center;
    background: var(--bg-raised);
    font-size: 1rem;
    font-weight: 700;
    color: var(--text-muted);
  }

  .future-portrait {
    filter: grayscale(0.5);
  }

  .tl-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }

  .tl-name {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text-primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .tl-sub {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  .tl-faction {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .tl-icon {
    width: 20px;
    height: 20px;
    object-fit: contain;
    flex-shrink: 0;
  }

  .tl-ratings {
    display: flex;
    gap: 4px;
    align-items: center;
  }

  .tl-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 28px;
    height: 20px;
    padding: 0 5px;
    border-radius: 4px;
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.03em;
    line-height: 1;
  }

  .tl-status-badge {
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 3px 7px;
    border-radius: 4px;
    white-space: nowrap;
  }

  .released-badge {
    background: rgba(107, 114, 128, 0.15);
    color: #9ca3af;
    border: 1px solid rgba(107, 114, 128, 0.2);
  }

  .soon-badge {
    background: rgba(250, 204, 21, 0.12);
    color: #facc15;
    border: 1px solid rgba(250, 204, 21, 0.25);
  }

  .future-badge-tl {
    background: rgba(168, 85, 247, 0.1);
    color: #a855f7;
    border: 1px solid rgba(168, 85, 247, 0.2);
  }

  /* ---- Responsive ---- */
  @media (max-width: 600px) {
    .spotlight-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .timeline-row {
      grid-template-columns: 24px 40px 1fr auto;
      gap: 8px;
    }

    .tl-faction,
    .tl-status-badge {
      display: none;
    }

    .tl-portrait {
      width: 40px;
      height: 40px;
    }

    .page-title {
      font-size: 1.3rem;
    }
  }

  @media (max-width: 400px) {
    .spotlight-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
