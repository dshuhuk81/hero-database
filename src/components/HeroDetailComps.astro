---
import { getHeroById } from "../data/heroes";

interface CompEntry {
  title: string;
  context?: string;
  description?: string;
  heroes: string[];
}

interface Props {
  comps?: CompEntry[];
  currentHeroId: string;
}

const { comps = [], currentHeroId } = Astro.props;

const R2_BASE = "https://pub-a33abfbc3135413881a1d8eb86543559.r2.dev";

function heroImg(id: string): string {
  const hero = getHeroById(id);
  const img = hero?.image;
  if (typeof img === "string" && img.length > 0) {
    if (img.startsWith("http://") || img.startsWith("https://")) return img;
    return img.startsWith("/") ? img : `/${img}`;
  }
  return `${R2_BASE}/heroes/${id}.webp`;
}

function heroName(id: string): string {
  return getHeroById(id)?.name ?? id;
}
---

{comps.length > 0 ? (
  <div class="hdc-list">
    {comps.map((comp) => (
      <div class="hdc-card">
        <div class="hdc-hero-strip">
          {comp.heroes.map((hid) => (
            <a
              href={`/heroes/${hid}`}
              class={`hdc-hero-slot${hid === currentHeroId ? " hdc-hero-slot--current" : ""}`}
              title={heroName(hid)}
            >
              <img
                class="hdc-hero-image"
                src={heroImg(hid)}
                alt={`${heroName(hid)} portrait`}
                width="512"
                height="512"
                loading="lazy"
                decoding="async"
              />
              <div class="hdc-name-overlay">
                <span class="hdc-hero-name">{heroName(hid)}</span>
              </div>
            </a>
          ))}
        </div>
      </div>
    ))}
  </div>
) : (
  <p class="hdc-empty">No team compositions listed yet.</p>
)}
